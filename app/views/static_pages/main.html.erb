<h1>Hello World!</h1>

<% @boards.each do |board| %>
		<fieldset>
			<%= link_to board.title, boards_show_path(board) %><br/>
			<% if board.description %>
				<small><%= board.description %></small>
			<% end %>
		</fieldset>
	<br/>
<% end %>
